version: '3.9'

services:
  frontend:
    image: blockscout-frontend:${FRONTEND_DOCKER_TAG:-latest}
    pull_policy: never
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
